import React from "react";

import { GenericForm } from "../components/GenericForm";

const Register: React.FC = ({}) => {
  return <GenericForm route="register" action="Sign Up" />;
};

export default Register;
